{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SifdaRecursoServicio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ entity.cantidad }}</td>
            </tr>
            <tr>
                <th>Costounitario</th>
                <td>{{ entity.costoUnitario }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.idTipoRecursoDependencia.idTipoRecurso.nombre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('sifdarecursoservicio', { 'idInf': entity.idInformeOrdenTrabajo.id }) }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('sifdarecursoservicio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
